kind = "web"
previewPath = "/"
title = "LOWNIE"
description = "A Swahili-first four-step service that verifies a phone number, collects a short loan-access story, accepts a configurable Paystack fee, and generates a downloadable Lownie ID ticket."
version = "1.0.0"
id = "artifacts/lownie"
router = "path"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21275

[services.development]
run = "pnpm --filter @workspace/lownie run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/lownie", "run", "build" ]
serve = "static"
publicDir = "artifacts/lownie/dist/public"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21275"
BASE_PATH = "/"
